next feature: ls-list

  A lightweight status skill that shows all active changes with their task completion ratios (e.g. add-user-auth — 3/5 tasks). It's the most common missing action when juggling multiple changes, and
  it's a tiny skill to build. Also it should show what phase we are in (propose, implement, verify, etc)
